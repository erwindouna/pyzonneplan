---
name: Dependabot auto-merge

# yamllint disable-line rule:truthy
on:
  pull_request_target:

# SAFETY: this workflow runs with a write token in the context of the base
# branch. Never add a checkout of the PR head ref here, and never execute
# anything from the PR itself.

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    name: 🤖 Auto-merge
    runs-on: ubuntu-latest
    if: >-
      github.event.pull_request.user.login == 'dependabot[bot]' &&
      github.repository == 'erwindouna/pyzonneplan'
    steps:
      - name: 🔍 Fetch Dependabot metadata
        id: metadata
        uses: dependabot/fetch-metadata@v3
      - name: ✅ Enable auto-merge for patch and minor updates
        if: >-
          steps.metadata.outputs.update-type == 'version-update:semver-patch' ||
          steps.metadata.outputs.update-type == 'version-update:semver-minor'
        run: gh pr merge --auto --squash "$PR_URL"
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
